{
    /**
    *  tsconfig.json 是 ts 编译器的配置文件，ts编译器可以根据它的信息来对代码进行编译
       "include" 用来指定哪些ts文件需要编译
           路径： ** 表示任意目录
                  * 表示任意文件
        "extends" 定义被继承的配置文件
    */
    "include": [
        "./src/**/*"
    ],
    "exclude": [
        "./src/test/**/*"
    ]
}